/**
 * Shared color palette for Claude Code-derived themes.
 * Used by both the Claude theme and the Blink (reduced-motion) theme.
 *
 * Colors extracted from Claude Code CLI v2.1.37 binary via `strings`.
 */
import { rgb } from 'ansis';

export const terracotta = rgb(215, 119, 87);
export const white = rgb(255, 255, 255);
export const subtle = rgb(80, 80, 80);
export const inactive = rgb(153, 153, 153);
export const success = rgb(78, 186, 101);
export const error = rgb(255, 107, 128);
export const warning = rgb(255, 193, 7);
